// useSession() hook is to get information on session in a client side component. (similar than auth() in a server side component). useSession need this api/auth/[...nextauth]/route.ts to get the session information.

export { GET, POST } from "@/lib/auth-no-edge";
